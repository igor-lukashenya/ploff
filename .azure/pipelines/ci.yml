# Azure DevOps — CI Pipeline
#
# Runs on every push and pull request to main/develop.
# Builds and tests all applications in the monorepo.
#
# TODO: Customize for your specific apps and tech stack.
# Docs: https://learn.microsoft.com/en-us/azure/devops/pipelines/

trigger:
  branches:
    include:
      - main
      - develop

pr:
  branches:
    include:
      - main
      - develop

pool:
  vmImage: 'ubuntu-latest'

variables:
  CI: 'true'
  # TODO: Add project-specific variables

stages:
  # ─── Lint Stage ──────────────────────────────────────────────────────────
  - stage: Lint
    displayName: 'Lint'
    jobs:
      - job: Lint
        displayName: 'Run Linters'
        steps:
          - checkout: self

          # TODO: Add linting steps for your tech stack
          # Example for Node.js:
          # - task: NodeTool@0
          #   inputs:
          #     versionSpec: '20.x'
          # - script: npm ci
          # - script: npm run lint

          - script: echo "TODO - Add linting steps"
            displayName: 'Placeholder'

  # ─── Test Stage ──────────────────────────────────────────────────────────
  - stage: Test
    displayName: 'Test'
    dependsOn: Lint
    jobs:
      - job: Test
        displayName: 'Run Tests'
        steps:
          - checkout: self

          # TODO: Add test steps for your tech stack
          # Example for Node.js:
          # - task: NodeTool@0
          #   inputs:
          #     versionSpec: '20.x'
          # - script: npm ci
          # - script: npm test
          # - task: PublishTestResults@2
          #   inputs:
          #     testResultsFormat: 'JUnit'
          #     testResultsFiles: '**/junit*.xml'

          - script: echo "TODO - Add test steps"
            displayName: 'Placeholder'

  # ─── Build Stage ─────────────────────────────────────────────────────────
  - stage: Build
    displayName: 'Build'
    dependsOn: Test
    jobs:
      - job: Build
        displayName: 'Build Applications'
        steps:
          - checkout: self

          # TODO: Add build steps
          # For Docker-based builds:
          # - task: Docker@2
          #   inputs:
          #     command: build
          #     Dockerfile: infra/docker/Dockerfile.api
          #     tags: $(Build.BuildId)

          - script: echo "TODO - Add build steps"
            displayName: 'Placeholder'
