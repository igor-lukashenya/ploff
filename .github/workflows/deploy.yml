# DEPRECATED - Use the new per-app deployment workflows instead:
#
#   deploy-app.yml     - Reusable per-app deploy (called by other workflows)
#   deploy-dev.yml     - Auto-deploy to dev on main merge
#   deploy-staging.yml - Auto-deploy to staging from release branches
#   release-please.yml - Auto-deploy to production on release
#
# Manual deployment:
#   Use workflow_dispatch on deploy-app.yml to deploy any app to any environment.
#
# See docs/adr/002-git-branching-strategy.md for the full flow.

name: Deploy (Legacy)

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is deprecated. Use deploy-app.yml instead.'
        required: false
        type: string

jobs:
  deprecated:
    name: Deprecated
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is deprecated."
          echo "Use the 'Deploy App' workflow instead (deploy-app.yml)."
          echo "See docs/adr/002-git-branching-strategy.md for details."
          exit 1
